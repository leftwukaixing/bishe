<?xml version="1.0" encoding="utf-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="manage.dao.e_manageDao">  
    <select id="select_e_manage"  parameterType="String" resultType="manage.entity.E_Manage">  
		select * from e_manage where tno_one = #{tno} or tno_two=#{tno} order by datetime
    </select>  
    <insert id="insert_e_manage"  parameterType="manage.entity.E_Manage">
    	insert into e_manage values
    	(#{cno} ,#{cname} ,#{datetime},#{time},#{address},#{tno_one},#{tname_one},#{tel_one},#{tno_two},#{tname_two},#{tel_two})
    </insert>
    <select id="select_all_e_manage" resultType="manage.entity.E_Manage">
    	select * from e_manage order by datetime
    </select>
    <update id="update_E_Manage" parameterType="manage.entity.E_Manage">
    	update e_manage set tno_one=#{tno_one},tname_one=#{tname_one},
    		tel_one=#{tel_one},tno_two=#{tno_two},tname_two=#{tname_two},tel_two=#{tel_two}
    		where cno=#{cno} and datetime=#{datetime} and address=#{address}
    </update>
    <delete id="delete_all_e_manage">
    	delete from e_manage
    </delete>
</mapper>